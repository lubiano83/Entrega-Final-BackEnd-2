<section class="products__table">
  <h1 class="titulo">Product Detail</h1>
  <div class="table__categories">
    {{#if product.available}}
      <div class="item__foto">
        <div>
          <a href="{{product.thumbnail.[0]}}" target="_blank">
            <img class="thumbnail" src="{{product.thumbnail.[0]}}" alt="{{product.title}}">
          </a>
        </div>
      </div>
      <div class="item__texto">
        <div class="id">Id: {{product._id}}</div>
        <div class="code">Code: {{product.code}}</div>
        <div class="category">Category: {{product.category}}</div>
        <div class="title">Title: {{product.title}}</div>
        <div class="price">Price: {{product.price}}</div>
        <div class="stock">Stock: {{product.stock}}</div>
        <div class="description">Description: {{product.description}}</div>
        <form action="/products/{{product._id}}" method="POST">
          <button type="submit">Add to Cart</button>
        </form>
      </div>
    {{else}}
      <div>No disponible</div>
    {{/if}}
  </div>
</section>
